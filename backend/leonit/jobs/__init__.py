"""Фоновые задачи: таблица ``jobs`` и воркер.

Обработчики регистрируются декоратором ``@job("kind")`` из ``registry``;
постановка и жизненный цикл — в ``service``; ``python -m leonit.jobs.worker``
запускает отдельный процесс-исполнитель из того же образа, что и API.
"""
