"""Медиа-пайплайн ответа кандидата и срок хранения записей.

Оригинал видео хранится как записал браузер; сервер только читает метаданные,
извлекает аудио для транскрибации и переупаковывает WebM для перемотки
(``ffmpeg.py``). Обработчики задач — в ``jobs.py``, логика — в ``service.py``.
"""
